Build this Figma frame in this project: "{{frameName}}" ({{frameType}}, {{size}}) from {{file}} · {{page}}.

{{designSource}}

Read the design before writing anything: `get_design_context` for every value, `get_variable_defs` for the token map, `get_screenshot` as the reference for the final visual diff, `download_assets` for every icon.

{{laws}}
